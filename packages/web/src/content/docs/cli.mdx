---
title: CLI
description: Exos Agent CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Exos Agent CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
exos-agent
```

But it also accepts commands as documented on this page. This allows you to interact with Exos Agent programmatically.

```bash
exos-agent run "Explain how closures work in JavaScript"
```

---

### tui

Start the Exos Agent terminal user interface.

```bash
exos-agent [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--auto"}</code></nobr>        |       | Auto-approve permissions that are not explicitly denied                 |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Exos Agent CLI also has the following commands.

---

### agent

Manage agents for Exos Agent.

```bash
exos-agent agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
exos-agent agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.exos-agent/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
exos-agent agent list
```

---

### attach

Attach a terminal to an already running Exos Agent backend server started via `serve` or `web` commands.

```bash
exos-agent attach [url]
```

This allows using the TUI with a remote Exos Agent backend. For example:

```bash
# Start the backend server for web/mobile access
exos-agent web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
exos-agent attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `EXOS_AGENT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `EXOS_AGENT_SERVER_USERNAME` or `exos-agent`) |

---

### auth

Command to manage credentials and login for providers.

```bash
exos-agent auth [command]
```

---

#### login

Exos Agent is powered by the provider list at [Models.dev](https://models.dev), so you can use `exos-agent auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/exos-agent/auth.json`.

```bash
exos-agent auth login
```

When Exos Agent starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
exos-agent auth list
```

Or the short version.

```bash
exos-agent auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
exos-agent auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
exos-agent github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
exos-agent github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
exos-agent github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
exos-agent mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
exos-agent mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
exos-agent mcp list
```

Or use the short version.

```bash
exos-agent mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
exos-agent mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
exos-agent mcp auth list
```

Or use the short version.

```bash
exos-agent mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
exos-agent mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
exos-agent mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
exos-agent models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
exos-agent models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Exos Agent.

```bash
exos-agent models --refresh
```

---

### run

Run exos-agent in non-interactive mode by passing a prompt directly.

```bash
exos-agent run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "exos-agent run"
exos-agent run Explain the use of context in Go
```

You can also attach to a running `exos-agent serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
exos-agent serve

# In another terminal, run commands that attach to it
exos-agent run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>    |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>   |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>    |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach to a running exos-agent server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `EXOS_AGENT_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `EXOS_AGENT_SERVER_USERNAME` or `exos-agent`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>     |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>  |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Show thinking blocks                                                       |
| <nobr><code>{"--auto"}</code></nobr>     |       | Auto-approve permissions that are not explicitly denied                    |

---

### serve

Start a headless Exos Agent server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
exos-agent serve
```

This starts an HTTP server that provides API access to exos-agent functionality without the TUI interface. Set `EXOS_AGENT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `exos-agent`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Exos Agent sessions.

```bash
exos-agent session [command]
```

---

#### list

List all Exos Agent sessions.

```bash
exos-agent session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Exos Agent session.

```bash
exos-agent session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Exos Agent sessions.

```bash
exos-agent stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
exos-agent export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Exos Agent share URL.

```bash
exos-agent import <file>
```

You can import from a local file or an Exos Agent share URL.

```bash
exos-agent import session.json
exos-agent import https://opncd.ai/s/abc123
```

---

### web

Start a headless Exos Agent server with a web interface.

```bash
exos-agent web
```

This starts an HTTP server and opens a web browser to access Exos Agent through a web interface. Set `EXOS_AGENT_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `exos-agent`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
exos-agent acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
exos-agent plugin <module>
```

Or use the alias.

```bash
exos-agent plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Exos Agent.

```bash
exos-agent pr <number>
```

---

### db

Database tools.

```bash
exos-agent db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
exos-agent db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
exos-agent debug [command]
```

---

### uninstall

Uninstall Exos Agent and remove all related files.

```bash
exos-agent uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates exos-agent to the latest version or a specific version.

```bash
exos-agent upgrade [target]
```

To upgrade to the latest version.

```bash
exos-agent upgrade
```

To upgrade to a specific version.

```bash
exos-agent upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The exos-agent CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Exos Agent can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `EXOS_AGENT_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `EXOS_AGENT_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `EXOS_AGENT_CONFIG`                     | string  | Path to config file                               |
| `EXOS_AGENT_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `EXOS_AGENT_CONFIG_DIR`                 | string  | Path to config directory                          |
| `EXOS_AGENT_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `EXOS_AGENT_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `EXOS_AGENT_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `EXOS_AGENT_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `EXOS_AGENT_PERMISSION`                 | string  | Inlined json permissions config                   |
| `EXOS_AGENT_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `EXOS_AGENT_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `EXOS_AGENT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `EXOS_AGENT_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `EXOS_AGENT_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `EXOS_AGENT_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `EXOS_AGENT_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `EXOS_AGENT_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `EXOS_AGENT_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `EXOS_AGENT_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `EXOS_AGENT_SERVER_USERNAME`            | string  | Override basic auth username (default `exos-agent`) |
| `EXOS_AGENT_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `EXOS_AGENT_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `EXOS_AGENT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `EXOS_AGENT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `EXOS_AGENT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `EXOS_AGENT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `EXOS_AGENT_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `EXOS_AGENT_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `EXOS_AGENT_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `EXOS_AGENT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `EXOS_AGENT_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `EXOS_AGENT_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `EXOS_AGENT_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `EXOS_AGENT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `EXOS_AGENT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `EXOS_AGENT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `EXOS_AGENT_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `EXOS_AGENT_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `EXOS_AGENT_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
