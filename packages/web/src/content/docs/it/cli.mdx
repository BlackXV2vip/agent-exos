---
title: CLI
description: Opzioni e comandi della CLI di Exos Agent.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di Exos Agent, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
exos-agent
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con Exos Agent in modo programmatico.

```bash
exos-agent run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di Exos Agent (interfaccia utente da terminale).

```bash
exos-agent [project]
```

#### Flag

| Flag                                     | Breve | Descrizione                                                              |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                               |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt da usare                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                       |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                          |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta su cui mettersi in ascolto                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di Exos Agent include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per Exos Agent.

```bash
exos-agent agent [command]
```

---

### attach

Collega un terminale a un backend server di Exos Agent già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
exos-agent attach [url]
```

Questo consente di usare la TUI con un backend Exos Agent remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
exos-agent web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
exos-agent attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory in cui avviare la TUI                                                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Crea un fork della sessione durante la continuazione (usa con `--continue` o `--session`)       |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `EXOS_AGENT_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `EXOS_AGENT_SERVER_USERNAME` o `exos-agent`) |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
exos-agent agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
exos-agent agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
exos-agent auth [command]
```

---

#### login

Exos Agent si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `exos-agent auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/exos-agent/auth.json`.

```bash
exos-agent auth login
```

Quando Exos Agent si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
exos-agent auth list
```

Oppure la versione corta.

```bash
exos-agent auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
exos-agent auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
exos-agent github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
exos-agent github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
exos-agent github run
```

##### Flag

| Flag                                  | Descrizione                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Evento GitHub mock per cui eseguire l'agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
exos-agent mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
exos-agent mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
exos-agent mcp list
```

Oppure la versione corta.

```bash
exos-agent mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
exos-agent mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
exos-agent mcp auth list
```

Oppure la versione corta.

```bash
exos-agent mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
exos-agent mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
exos-agent mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
exos-agent models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
exos-agent models anthropic
```

#### Flag

| Flag                                    | Descrizione                                        |
| --------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Aggiorna la cache modelli da models.dev            |
| <nobr><code>{"--verbose"}</code></nobr> | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in Exos Agent.

```bash
exos-agent models --refresh
```

---

### run

Esegui exos-agent in modalità non interattiva passando un prompt direttamente.

```bash
exos-agent run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "exos-agent run"
exos-agent run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `exos-agent serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
exos-agent serve

# In un altro terminale, esegui comandi che si collegano ad esso
exos-agent run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Il comando da eseguire; usa message per gli argomenti                                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`)                        |
| <nobr><code>{"--share"}</code></nobr>    |       | Condividi la sessione                                                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                                                 |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File da allegare al messaggio                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: default (formattato) o json (eventi JSON grezzi)                                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)                            |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach a un server exos-agent in esecuzione (es. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `EXOS_AGENT_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `EXOS_AGENT_SERVER_USERNAME` o `exos-agent`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory di esecuzione, o percorso sul server remoto durante il collegamento                   |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modello (sforzo di ragionamento specifico del provider)                            |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostra blocchi di pensiero                                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta per il server locale (di default una porta casuale)                                       |

---

### serve

Avvia un server Exos Agent headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
exos-agent serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di exos-agent senza la TUI. Imposta `EXOS_AGENT_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `exos-agent`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni Exos Agent.

```bash
exos-agent session [command]
```

---

#### list

Elenca tutte le sessioni Exos Agent.

```bash
exos-agent session list
```

##### Flag

| Flag                                      | Breve | Descrizione                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limita alle N sessioni più recenti   |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni Exos Agent.

```bash
exos-agent stats
```

#### Flag

| Flag                                    | Descrizione                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| <nobr><code>{"--tools"}</code></nobr>   | Numero di strumenti da mostrare (all)                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| <nobr><code>{"--project"}</code></nobr> | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
exos-agent export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione Exos Agent.

```bash
exos-agent import <file>
```

Puoi importare da un file locale o da un URL di condivisione Exos Agent.

```bash
exos-agent import session.json
exos-agent import https://opncd.ai/s/abc123
```

---

### web

Avvia un server Exos Agent headless con interfaccia web.

```bash
exos-agent web
```

Avvia un server HTTP e apre un browser per accedere a Exos Agent tramite interfaccia web. Imposta `EXOS_AGENT_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `exos-agent`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
exos-agent acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag                                     | Descrizione                         |
| ---------------------------------------- | ----------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directory di lavoro                 |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto    |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla Exos Agent e rimuove tutti i file correlati.

```bash
exos-agent uninstall
```

#### Flag

| Flag                                        | Breve | Descrizione                                  |
| ------------------------------------------- | ----- | -------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantieni i file di configurazione            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Mantieni dati di sessione e snapshot         |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna exos-agent all'ultima versione o a una versione specifica.

```bash
exos-agent upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
exos-agent upgrade
```

Per aggiornare a una versione specifica:

```bash
exos-agent upgrade v0.1.48
```

#### Flag

| Flag                                   | Breve | Descrizione                                               |
| -------------------------------------- | ----- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di exos-agent accetta i seguenti flag globali.

| Flag                                       | Breve | Descrizione                            |
| ------------------------------------------ | ----- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostra l'help                          |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Stampa il numero di versione           |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Stampa i log su stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

Exos Agent può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `EXOS_AGENT_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `EXOS_AGENT_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `EXOS_AGENT_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `EXOS_AGENT_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `EXOS_AGENT_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `EXOS_AGENT_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `EXOS_AGENT_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `EXOS_AGENT_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `EXOS_AGENT_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `EXOS_AGENT_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `EXOS_AGENT_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `EXOS_AGENT_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `EXOS_AGENT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `EXOS_AGENT_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `EXOS_AGENT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `EXOS_AGENT_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `EXOS_AGENT_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `EXOS_AGENT_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `EXOS_AGENT_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `EXOS_AGENT_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `EXOS_AGENT_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `exos-agent`)     |
| `EXOS_AGENT_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                             |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `EXOS_AGENT_EXPERIMENTAL`                         | boolean | Abilita funzionalità sperimentali sotto il flag globale |
| `EXOS_AGENT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                                  |
| `EXOS_AGENT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI                     |
| `EXOS_AGENT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms               |
| `EXOS_AGENT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM                |
| `EXOS_AGENT_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir                   |
| `EXOS_AGENT_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                                 |
| `EXOS_AGENT_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale                      |
| `EXOS_AGENT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                                 |
| `EXOS_AGENT_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali                   |
| `EXOS_AGENT_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita TY LSP per i file python                        |
| `EXOS_AGENT_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                                       |
| `EXOS_AGENT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Abilita task subagent in background                     |
| `EXOS_AGENT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Abilita sistema eventi sperimentale                     |
| `EXOS_AGENT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Abilita percorso nativo di richiesta LLM                |
| `EXOS_AGENT_EXPERIMENTAL_PARALLEL`                | boolean | Abilita esecuzione parallela della ricerca web          |
| `EXOS_AGENT_EXPERIMENTAL_SCOUT`                   | boolean | Abilita subagent Scout                                  |
| `EXOS_AGENT_EXPERIMENTAL_WORKSPACES`              | boolean | Abilita supporto workspace                              |
